# Для перекладу тексту
deep-translator

# Для відображення прогресу
tqdm

# Для кольорових та звичайних таблиць
prettytable

# Якщо використовуєш utils та data як пакети — вони повинні бути локальними модулями,
# тому їх не треба встановлювати через pip

# Додатково (за потреби, якщо з’являться помилки кольорів/терміналу)
colorama
